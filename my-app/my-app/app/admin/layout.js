export const metadata = {
  title: 'Admin — AMI CMS',
  description: 'Content Management System for Artha Mitra Interdata website',
};

export default function AdminLayout({ children }) {
  return children;
}
